#include <stdio.h>


int main(int argc, char *argv[])
{
	int count;
	printf("\%s\n", argv[0]);

	if (arg > 1)
	{
		for (count = 1; count < argc; count++)
	{
		printf("argc[%d] = %s\n", count, argc[count]);
	}
	}
	else
	{
		return (0);
	}
		return (0):
}
