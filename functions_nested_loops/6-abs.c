#include "main.h"
/**
 * _abs - compute the absolute value of the integer
 * @r: the integer who needs to be computed
 * Return: Always r ou -r
 */

int _abs(int r)
{
	if (r >= 0)
{
	return (r);
}
	else
{
	return (-r);
}
}
