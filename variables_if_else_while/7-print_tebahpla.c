#include <stdio.h>
/**
 * main - is the entry point
 * Return: Always 0
 */

int main(void)
{
	char n = 'z';
	char i = 'a';

	for (n = 'z'; n >= 'a'; n--)
{
	putchar(n);
}
{
	putchar('\n');
}
	return (0);
}
